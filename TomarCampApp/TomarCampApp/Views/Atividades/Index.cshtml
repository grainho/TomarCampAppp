@model IEnumerable<TomarCampApp.Models.Atividades>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de Atividades</h2>
@if (User.IsInRole("Func"))
{
<p>
    @Html.ActionLink("Criar Nova Atividade", "Create") |
    @Html.ActionLink("Criar Nova Concretização", "Create", "Concretizacaos", null, null)
</p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)

        </th>
        <th>
            @Html.DisplayNameFor(model => model.dataCriacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.materiais)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Nome, "Details", new { id = item.ID })
            @if (User.IsInRole("Func"))
            {
                <text>
                     | @Html.ActionLink("Edit", "Edit", new { id = item.ID })
                </text>
            }
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dataCriacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.materiais)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
       
    </tr>
}

</table>
